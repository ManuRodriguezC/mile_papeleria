---
const {slug, image, title} = Astro.props;
---

<a href={`/producto/${slug}`} class="">
  <div
    class="flex flex-col justify-center items-center bg-white rounded-lg shadow-md p-4 hover:shadow-lg transition-shadow">
    <img
      src={image}
      alt={title}
      class="w-auto h-[300px] object-cover"
      loading="lazy"
    />
    <h2 class="text-center text-base font-bold mb-2">{title}</h2>
  </div>
</a>